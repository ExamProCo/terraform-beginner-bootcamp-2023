#!/usr/bin/env bash

# install baseline packages for Linux and install Curl
sudo apt-get update && sudo apt-get install -y gnupg software-properties-common

# Download GPG key from Hashicorp
wget -O- https://apt.releases.hashicorp.com/gpg | \
gpg --dearmor | \
sudo tee /usr/share/keyrings/hashicorp-archive-keyring.gpg

# Add Hashicorp GPG to keyring
gpg --no-default-keyring \
--keyring /usr/share/keyrings/hashicorp-archive-keyring.gpg \
--fingerprint

# Adding the Hashicorp Package Repositry to apt-get install
echo "deb [signed-by=/usr/share/keyrings/hashicorp-archive-keyring.gpg] \
https://apt.releases.hashicorp.com $(lsb_release -cs) main" | \
sudo tee /etc/apt/sources.list.d/hashicorp.list

# Ensuring our sources are to up to date
sudo apt update

# We are install terraform cli
sudo apt-get install terraform -y